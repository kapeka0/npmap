// Lazily-loaded chunk, only reachable by following the reference in bundle.js.
export const banner = "FakeLib v3 — the definitely-real npm library";
export function greet() {
  return "hello from FakeLib v3";
}
